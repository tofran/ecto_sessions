defmodule EctoSessionsTest do
  use ExUnit.Case
  doctest EctoSessions

  # TODO: improve these tests / create integration tests,
  # unfortunally at the moment most features of this specific module need
  # an Ecto.Repo. Avoid it by having something mocked replace with unit tests
  # with a proper database

  test "sample test" do
    assert true
  end
end
